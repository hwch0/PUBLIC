<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kr.pub.dao.ChatDAO">
	<insert id="insert">
	    INSERT INTO chat(type, sender, receiver, message, seatNo, time)
		VALUES (#{type}, #{sender},#{receiver}, #{message},#{seatNo}, #{time})
	</insert>
	
	<select id="getChatLIst" resultType="com.kr.pub.dto.ChatDTO">
		SELECT * FROM chat
		WHERE TRUNC(time) = TRUNC(SYSDATE)
		ORDER BY time ASC
	</select>
	<select id="getChatLIstById" resultType="com.kr.pub.dto.ChatDTO">
		SELECT *
		FROM chat
		WHERE TRUNC(time) = TRUNC(SYSDATE)
		  AND
		  AND user_Id IN (#{userId}, 'admin')
		  AND seatNo = #{seatNo}
		ORDER BY time ASC;
	</select>
</mapper>